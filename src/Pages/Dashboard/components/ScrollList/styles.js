import styled from 'styled-components'

export const Container = styled.div`
  height: 80px;
  width: 200px;
  overflow-y: scroll;

  background: rgba(0, 0, 0, 0.2);
  padding: 10px;

  .client_list {
    list-style: none;
  }
`
